---
/**
 * HOMEPAGE ASSEMBLY
 * This is the main entry point of the site. It handles:
 * 1. Hybrid Data Fetching: Combines portfolio.json data with Astro Content Collections.
 * 2. Specialized Filtering: Separates 'Featured' skills and 'Featured' projects for a clean grid layout.
 * 3. Component Orchestration: Integrates the Interactive Terminal, Skills Manager, and Timeline sections.
 */
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import ProjectCard from '../components/ProjectCard.jsx';
import InteractiveList from '../components/InteractiveList.jsx';
import InteractiveTerminal from '../components/InteractiveTerminal.jsx';
import data from '../data/portfolio.json';
import details from '../data/details.json';
import SkillsManager from '../components/SkillsManager.jsx';
import { getCollection } from 'astro:content';

// Fetch Blog Posts (Sort by newest)
const posts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
).slice(0, 3);

// Fetch Projects (Filter by visibility & featured, limit to 3)
const projects = (await getCollection('projects'))
	.filter(p => p.data.visible !== false && p.data.featured === true)
	.slice(0, 3);

// Attach HTML content to work/education items
function enhanceData(items: any[]) {
    if (!items) return [];
    return items.map((item: any) => {
        if (item.detailId && details[item.detailId as keyof typeof details]) {
            const detailContent = details[item.detailId as keyof typeof details] as any;
            return { 
                ...item, 
                html: detailContent.content,
                achievements: detailContent.achievements || [] 
            };
        }
        return item;
    });
}

const enhancedWork = enhanceData(data.work);
const enhancedEducation = enhanceData(data.education);

// Filter skills for the featured grid (only show categories that have featured items)
const featuredSkills = data.skills
	.map(cat => ({
		...cat,
		items: cat.items.filter(i => i.featured)
	}))
	.filter(cat => cat.items.length > 0);
---

<!doctype html>
<html lang="en" class="scroll-smooth">
	<head>
		<BaseHead title={`${data.basics.name} | ${data.basics.role}`} description={data.basics.tagline} />
	</head>
	<body class="bg-white dark:bg-darkBg text-slate-900 dark:text-slate-200 font-sans transition-colors duration-300 flex flex-col min-h-screen">
		<Header />
		
		<main class="flex-grow">
			
			<!-- =========================================
			     HERO SECTION
			     ========================================= -->
			<section id="hero" class="max-w-6xl mx-auto px-4 py-20 md:py-28">
				<div class="grid lg:grid-cols-2 gap-12 items-center">
					<div class="space-y-6">
						<div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-slate-100 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 text-xs font-medium text-slate-600 dark:text-slate-300 w-fit">
							<span class="w-2 h-2 rounded-full bg-green-500 animate-pulse"></span>
							{data.basics.status}
						</div>
						<h1 class="text-5xl md:text-6xl font-bold tracking-tight text-slate-900 dark:text-white">
							{data.basics.name}
						</h1>
						<h2 class="text-2xl md:text-3xl text-primary font-medium">
							{data.basics.role} <span class="text-slate-400 dark:text-slate-500">&</span> {data.basics.subRole}
						</h2>
						<p class="text-lg text-slate-600 dark:text-slate-400 leading-relaxed max-w-xl">
							{data.basics.tagline}
						</p>
						<div class="flex flex-wrap gap-4 pt-4">
							<a href="/resume.pdf" class="bg-primary text-white px-6 py-3 rounded-lg font-medium hover:bg-blue-600 transition-colors shadow-lg shadow-blue-500/20">Download CV</a>
							<a href="/projects" class="border border-slate-300 dark:border-slate-700 text-slate-700 dark:text-slate-300 px-6 py-3 rounded-lg font-medium hover:bg-slate-50 dark:hover:bg-slate-800 transition-colors">View Projects</a>
						</div>
						<!-- Social Links -->
						<div class="flex gap-4 pt-4">
							<a href="https://github.com/birajgtm" target="_blank" rel="noopener noreferrer" class="text-slate-600 dark:text-slate-400 hover:text-primary transition-colors" aria-label="GitHub">
								<svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24"><path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/></svg>
							</a>
							<a href="https://linkedin.com/in/birajgtm" target="_blank" rel="noopener noreferrer" class="text-slate-600 dark:text-slate-400 hover:text-primary transition-colors" aria-label="LinkedIn">
								<svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24"><path d="M19 0h-14c-2.761 0-5 2.239-5 5v14c0 2.761 2.239 5 5 5h14c2.762 0 5-2.239 5-5v-14c0-2.761-2.238-5-5-5zm-11 19h-3v-11h3v11zm-1.5-12.268c-.966 0-1.75-.79-1.75-1.764s.784-1.764 1.75-1.764 1.75.79 1.75 1.764-.783 1.764-1.75 1.764zm13.5 12.268h-3v-5.604c0-3.368-4-3.113-4 0v5.604h-3v-11h3v1.765c1.396-2.586 7-2.777 7 2.476v6.759z"/></svg>
							</a>
						</div>
					</div>
					<!-- Terminal Graphic (Interactive) -->
					<div class="flex justify-center items-center mt-8 lg:mt-0">
						<InteractiveTerminal client:load data={data} />
					</div>
				</div>
			</section>

			<!-- =========================================
			     SKILLS
			     ========================================= -->
			<section id="skills" class="bg-slate-50 dark:bg-slate-800/50 border-y border-slate-200 dark:border-slate-800">
				<div class="max-w-5xl mx-auto px-4 py-16">
					<h2 class="text-3xl font-bold text-slate-900 dark:text-white mb-10 flex items-center gap-3">
						<span class="text-primary">⚙️</span> Technical Competencies
					</h2>
					<div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
						{featuredSkills.map((category) => (
							<div class="bg-white dark:bg-slate-900 p-6 rounded-xl border border-slate-200 dark:border-slate-700 shadow-sm relative overflow-hidden group/cat">
								{/* Subtle category decoration */}
								<div class="absolute top-0 right-0 w-16 h-16 bg-primary/5 rounded-bl-full -mr-4 -mt-4 group-hover/cat:bg-primary/10 transition-colors"></div>

								<h4 class="text-lg font-semibold mb-6 text-slate-900 dark:text-white flex items-center gap-2 relative z-10">
									{category.category === "OS & Devices" ? "💻" : 
									 category.category === "Networking & Security" ? "🔒" :
									 category.category === "Systems & Cloud" ? "☁️" :
									 category.category === "Virtualization & Infra" ? "🗄️" :
									 category.category === "ITSM & Operations" ? "🛠️" :
									 category.category === "Automation & Monitoring" ? "🤖" : "⚡"} {category.category}
								</h4>
								<div class="space-y-4 relative z-10">
									{category.items.map((skill) => (
										<div class="flex items-center gap-3">
											<div class="w-8 h-8 flex items-center justify-center rounded-lg bg-slate-50 dark:bg-slate-800 border border-slate-100 dark:border-slate-700 transition-colors shadow-sm text-lg flex-shrink-0">
												{skill.name.includes("Windows") || skill.name.includes("Server") ? "🪟" : 
												 skill.name.includes("macOS") ? "🍎" : 
												 skill.name.includes("Linux") ? "🐧" :
												 skill.name.includes("FortiGate") ? "🛡️" :
												 skill.name.includes("Azure") || skill.name.includes("Intune") ? "☁️" :
												 skill.name.includes("VMware") ? "💾" :
												 skill.name.includes("ServiceNow") || skill.name.includes("Jira") ? "📂" :
												 skill.name.includes("Python") ? "🐍" :
												 skill.name.includes("Bash") || skill.name.includes("SQL") ? "⌨️" :
												 skill.name.includes("JavaScript") || skill.name.includes("React") ? "⚛️" : "⚡"}
											</div>
											<div class="flex-grow min-w-0">
												<div class="flex flex-col">
													<span class="text-sm font-semibold text-slate-700 dark:text-slate-200 leading-none mb-2 truncate">{skill.name}</span>
													<div class="w-full max-w-[140px] h-2.5 bg-slate-100 dark:bg-slate-800 rounded-full overflow-hidden border-2 border-slate-300 dark:border-slate-600 shadow-inner">
														{(() => {
															const percent = skill.level.toLowerCase() === 'expert' ? '100%' : 
																			skill.level.toLowerCase() === 'advanced' ? '75%' : 
																			skill.level.toLowerCase() === 'intermediate' ? '50%' : '25%';
															return (
																<div 
																	class="h-full bg-emerald-500 rounded-full transition-all duration-1000 shadow-[0_0_8px_rgba(16,185,129,0.3)]" 
																	style={`width: ${percent}`}
																></div>
															);
														})()}
													</div>
												</div>
											</div>
										</div>
									))}
								</div>
							</div>
						))}
					</div>

					<SkillsManager client:load allSkills={data.skills} />
				</div>
			</section>

			<!-- =========================================
			     PROFESSIONAL TIMELINE (Interactive)
			     ========================================= -->
			<section id="experience" class="max-w-4xl mx-auto px-4 py-20">
				
				<!-- 1. EXPERIENCE -->
				<div class="mb-20">
					<h2 class="text-3xl font-bold text-slate-900 dark:text-white mb-10 flex items-center gap-3">
						<span class="text-primary">💼</span> Professional Experience
					</h2>
					<!-- React Component handles the click & modal -->
					<InteractiveList client:load items={enhancedWork} type="work" />
				</div>

				<!-- 2. EDUCATION -->
				<div class="mb-20">
					<h2 class="text-3xl font-bold text-slate-900 dark:text-white mb-10 flex items-center gap-3">
						<span class="text-primary">🎓</span> Education
					</h2>
					<!-- React Component handles the click & modal -->
					<InteractiveList client:load items={enhancedEducation} type="education" />
				</div>

				<!-- 3. CERTIFICATIONS -->
				<div id="certifications">
					<h2 class="text-3xl font-bold text-slate-900 dark:text-white mb-10 flex items-center gap-3">
						<span class="text-primary">📜</span> Certifications
					</h2>
					
					<div class="flex flex-col gap-4">
						{data.certs.map((cert) => (
							<a 
								href={cert.url} 
								target="_blank"
								class="group flex items-center gap-4 p-4 rounded-xl bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-700 hover:border-primary hover:shadow-lg transition-all duration-300 relative overflow-hidden"
							>
								<!-- Hover Effect Background -->
								<div class="absolute inset-0 bg-primary/5 opacity-0 group-hover:opacity-100 transition-opacity"></div>

								<!-- Icon / Issuer Badge -->
								<div class="flex-shrink-0 w-12 h-12 flex items-center justify-center rounded-lg bg-slate-50 dark:bg-slate-800 border border-slate-100 dark:border-slate-700 group-hover:scale-105 transition-transform z-10">
									<span class="text-2xl">
										{cert.issuer.includes('Microsoft') ? '🟦' : 
										 cert.issuer.includes('Cisco') ? '📡' : 
										 cert.issuer.includes('Fortinet') ? '🛡️' : 
										 '🏅'}
									</span>
								</div>

								<!-- Content -->
								<div class="flex-grow min-w-0 z-10">
									<h3 class="text-base md:text-lg font-bold text-slate-900 dark:text-white truncate group-hover:text-primary transition-colors">
										{cert.name}
									</h3>
									<div class="flex items-center gap-2 text-sm text-slate-500 dark:text-slate-400">
										<span>{cert.issuer}</span>
										<span class="w-1 h-1 rounded-full bg-slate-300"></span>
										<span>{cert.date}</span>
									</div>
								</div>

								<!-- Verify Action -->
								<div class="hidden sm:flex items-center gap-1 text-xs font-bold text-primary opacity-0 group-hover:opacity-100 -translate-x-2 group-hover:translate-x-0 transition-all duration-300 z-10">
									VERIFY <span>↗</span>
								</div>
								<div class="sm:hidden text-slate-300 group-hover:text-primary z-10">↗</div>
							</a>
						))}
					</div>
				</div>
			</section>

			<!-- =========================================
			     PROJECTS
			     ========================================= -->
			<section id="projects" class="bg-slate-50 dark:bg-slate-800/50 border-y border-slate-200 dark:border-slate-800">
				<div class="max-w-5xl mx-auto px-4 py-20">
					<div class="flex items-center justify-between mb-12">
						<h2 class="text-3xl font-bold text-slate-900 dark:text-white">Featured Projects</h2>
						<a href="/projects" class="hidden md:inline-flex text-primary font-medium hover:underline">View All →</a>
					</div>
					<div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
						{projects.map((project) => (
							<ProjectCard 
								client:visible 
								title={project.data.title}
								description={project.data.description}
								tags={project.data.stack}
								link={`/projects/${project.id.replace(/\.md$/, '')}`}
								image={project.data.heroImage}
								fallbackIcon={project.data.category === 'IT' ? 'shield' : (project.data.stack?.[0]?.toLowerCase() === 'python' ? 'python' : 'code')}
								category={project.data.category}
							/>
						))}
					</div>
				</div>
			</section>

			<!-- =========================================
			     LATEST BLOG POSTS (New Premium Design)
			     ========================================= -->
			<section id="blog" class="max-w-5xl mx-auto px-4 py-20">
				<div class="flex items-center justify-between mb-8">
					<h2 class="text-2xl font-bold text-slate-900 dark:text-white">Latest Blog Posts</h2>
					<a href="/blog" class="text-primary hover:underline">Read Blog →</a>
				</div>
				<div class="grid md:grid-cols-3 gap-6">
					{posts.map((post) => (
						<a href={`/blog/${post.id.replace(/\.md$/, '')}/`} class="group relative flex flex-col h-full bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-700 p-6 hover:shadow-2xl hover:border-primary/30 hover:-translate-y-1 transition-all duration-300 overflow-hidden">
							
							<!-- 1. Gradient Overlay (Appears on Hover) -->
							<div class="absolute inset-0 bg-gradient-to-br from-primary/5 via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>

							<!-- 2. Header: Date & Icon -->
							<div class="relative flex items-center justify-between mb-4 z-10">
								<span class="text-xs font-mono font-medium text-slate-500 dark:text-slate-400 bg-slate-100 dark:bg-slate-800 px-2 py-1 rounded border border-slate-200 dark:border-slate-700 group-hover:border-primary/30 transition-colors">
									{post.data.pubDate.toLocaleDateString()}
								</span>
								
								<!-- Document Icon -->
								<svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 text-slate-300 dark:text-slate-600 group-hover:text-primary transition-colors duration-300" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
									<path d="M14.5 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7.5L14.5 2z"/>
									<polyline points="14 2 14 8 20 8"/>
								</svg>
							</div>

							<!-- 3. Content -->
							<div class="relative flex-grow z-10">
								<h3 class="text-lg font-bold text-slate-900 dark:text-white mb-3 group-hover:text-primary transition-colors leading-tight">
									{post.data.title}
								</h3>
								<p class="text-sm text-slate-600 dark:text-slate-400 line-clamp-3 leading-relaxed mb-6">
									{post.data.description}
								</p>
							</div>

							<!-- 4. Footer: 'Read Article' Link -->
							<div class="relative mt-auto flex items-center text-primary font-bold text-sm z-10">
								Read Article 
								<span class="ml-2 transform group-hover:translate-x-1 transition-transform duration-300">→</span>
							</div>
						</a>
					))}
				</div>
			</section>

			<!-- =========================================
			     CONTACT SECTION
			     ========================================= -->
			<section id="contact" class="bg-gradient-to-br from-primary/5 via-blue-50 dark:via-slate-900 to-slate-50 dark:to-darkBg border-y border-slate-200 dark:border-slate-800">
				<div class="max-w-4xl mx-auto px-4 py-20 text-center">
					<h2 class="text-3xl md:text-4xl font-bold text-slate-900 dark:text-white mb-4">
						Let's Work Together
					</h2>
					<p class="text-lg text-slate-600 dark:text-slate-400 mb-8 max-w-2xl mx-auto">
						I'm currently {data.basics.status.toLowerCase()} and available for new opportunities. Feel free to reach out!
					</p>
					
					<!-- Availability Badge -->
					<div class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-green-100 dark:bg-green-900/30 border border-green-200 dark:border-green-800 text-green-700 dark:text-green-400 font-medium mb-8">
						<span class="w-2 h-2 rounded-full bg-green-500 animate-pulse"></span>
						{data.basics.status}
					</div>
					
					<!-- Contact Methods -->
					<div class="flex flex-col sm:flex-row gap-4 justify-center items-center mb-8">
						<a 
							href={`mailto:${data.basics.email}`}
							class="group flex items-center gap-3 px-6 py-3 bg-primary text-white rounded-lg font-medium hover:bg-blue-600 transition-all shadow-lg shadow-primary/20 hover:shadow-xl"
						>
							<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
								<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
							</svg>
							{data.basics.email}
						</a>
						
						<a 
							href={`tel:${data.basics.phone}`}
							class="flex items-center gap-3 px-6 py-3 border-2 border-slate-300 dark:border-slate-700 text-slate-700 dark:text-slate-300 rounded-lg font-medium hover:bg-slate-50 dark:hover:bg-slate-800 transition-all"
						>
							<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
								<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z" />
							</svg>
							{data.basics.phone}
						</a>
					</div>
					
					<!-- Social Links -->
					<div class="flex gap-4 justify-center">
						<a href="https://github.com/birajgtm" target="_blank" rel="noopener noreferrer" class="p-3 rounded-full bg-slate-100 dark:bg-slate-800 text-slate-600 dark:text-slate-400 hover:text-primary hover:bg-slate-200 dark:hover:bg-slate-700 transition-colors" aria-label="GitHub">
							<svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24"><path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/></svg>
						</a>
						<a href="https://linkedin.com/in/birajgtm" target="_blank" rel="noopener noreferrer" class="p-3 rounded-full bg-slate-100 dark:bg-slate-800 text-slate-600 dark:text-slate-400 hover:text-primary hover:bg-slate-200 dark:hover:bg-slate-700 transition-colors" aria-label="LinkedIn">
							<svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24"><path d="M19 0h-14c-2.761 0-5 2.239-5 5v14c0 2.761 2.239 5 5 5h14c2.762 0 5-2.239 5-5v-14c0-2.761-2.238-5-5-5zm-11 19h-3v-11h3v11zm-1.5-12.268c-.966 0-1.75-.79-1.75-1.764s.784-1.764 1.75-1.764 1.75.79 1.75 1.764-.783 1.764-1.75 1.764zm13.5 12.268h-3v-5.604c0-3.368-4-3.113-4 0v5.604h-3v-11h3v1.765c1.396-2.586 7-2.777 7 2.476v6.759z"/></svg>
						</a>
					</div>
				</div>
			</section>

		</main>
		<Footer />
	</body>
</html>